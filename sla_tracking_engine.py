def start_sla_timer(incident_id, phase):
    print(f"DEBUG: SLA timer started for incident {incident_id}, phase {phase}")
    return True

def reset_sla_timer(incident_id, phase):
    print(f"DEBUG: SLA timer reset for incident {incident_id}, phase {phase}")
    return True
